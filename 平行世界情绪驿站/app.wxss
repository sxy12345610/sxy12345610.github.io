/* ============================================
   全局样式 - 平行世界情绪驿站
   ============================================ */

/* 页面基础样式 */
page {
  font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
  background: linear-gradient(135deg, #fdf6e3 0%, #fce4ec 50%, #e8f5e9 100%);
  min-height: 100vh;
  overflow-x: hidden;
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  padding-top: env(safe-area-inset-top);
}

view, text, image, button {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 滚动容器 */
.page-scroll {
  width: 100%;
  height: 100vh;
  -webkit-overflow-scrolling: touch;
  box-sizing: border-box;
}

.container {
  max-width: 420px;
  margin: 0 auto;
  padding: 20rpx;
  padding-bottom: 180rpx;
  position: relative;
  min-height: 100vh;
  box-sizing: border-box;
}

/* ============================================
   动画关键帧
   ============================================ */

/* 页面淡入动画 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes pageFadeIn {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes pageSlideOut {
  from { opacity: 1; transform: translateX(0); }
  to { opacity: 0; transform: translateX(-20px); }
}

/* 页面切换动画 */
.page-enter {
  animation: pageEnter 0.3s ease-out forwards;
}

.page-leave {
  animation: pageLeave 0.2s ease-in forwards;
}

@keyframes pageEnter {
  from {
    opacity: 0;
    transform: translateY(20rpx) scale(0.98);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes pageLeave {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
  to {
    opacity: 0;
    transform: translateY(-20rpx) scale(0.98);
  }
}

/* 漂浮粒子动画 */
.particles {
  position: fixed;
  top: env(safe-area-inset-top);
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0;
}

.particle {
  position: absolute;
  width: 4px;
  height: 4px;
  background: rgba(255, 182, 193, 0.5);
  border-radius: 50%;
  animation: float 8s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0) translateX(0); opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 1; }
  100% { transform: translateY(-100vh) translateX(100px); opacity: 0; }
}

/* 卡片滑入动画 */
@keyframes cardSlideIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes cardBounce {
  0% { transform: scale(1); }
  50% { transform: scale(1.12); }
  100% { transform: scale(1.08); }
}

/* 弹幕漂浮动画 */
@keyframes floatMessage {
  0% { transform: translateX(100vw); }
  100% { transform: translateX(-100%); }
}

/* 闪光动画 */
@keyframes sparkle {
  0% { transform: scale(0) translateY(0); opacity: 1; }
  100% { transform: scale(1) translateY(-50px); opacity: 0; }
}

/* 瓶子漂浮动画 */
@keyframes bottleFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

/* ============================================
   漂浮弹幕样式（全局）
   ============================================ */

.messages-wall {
  position: fixed;
  bottom: 200rpx;
  left: 0;
  width: 100%;
  height: 600rpx;
  overflow: hidden;
  pointer-events: none;
  z-index: 1;
}

.message-item {
  position: absolute;
  white-space: nowrap;
  font-size: 24rpx;
  color: rgba(93, 78, 109, 0.15);
  opacity: 0.15;
  animation: floatMessage 15s linear infinite;
}

/* ============================================
   卡片基础样式
   ============================================ */

.card {
  position: relative;
  z-index: 10;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 32rpx;
  padding: 32rpx;
  min-height: 400rpx;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  word-break: break-word;
  white-space: normal;
  line-height: 1.8;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.08);
}

/* ============================================
   翻牌动画样式
   ============================================ */

.flip-card {
  perspective: 2000rpx;
  position: relative;
}

.flip-card-inner {
  position: relative;
  width: 100%;
  height: 100%;
  transition: transform 0.6s;
  transform-style: preserve-3d;
}

.flip-card.flipped .flip-card-inner {
  transform: rotateY(180deg);
}

.card-front,
.card-back {
  position: absolute;
  width: 100%;
  height: 100%;
  backface-visibility: hidden;
  -webkit-backface-visibility: hidden;
  border-radius: 32rpx;
  padding: 32rpx;
}

.card-front {
  background: linear-gradient(135deg, #ffb7b2 0%, #ffecd2 100%);
}

.card-back {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  transform: rotateY(180deg);
}

/* ============================================
   夜间模式样式（全局）
   ============================================ */

page.night-mode {
  background: linear-gradient(135deg, #0d0d1a 0%, #1a1a3e 50%, #16213e 100%);
}

.night-mode .container {
  background: transparent;
}

.night-mode .home-title {
  color: #e8e8e8;
}

.night-mode .home-subtitle {
  color: #a0a0a0;
}

.night-mode .emotion-card {
  background: rgba(255, 255, 255, 0.08);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
}

.night-mode .emotion-name {
  color: #e8e8e8;
}

.night-mode .page-title {
  color: #e8e8e8;
}

.night-mode .story-text {
  color: #e8e8e8;
}

.night-mode .challenge-label {
  color: #b0b0b0;
}

.night-mode .challenge-text {
  color: #e8e8e8;
}

.night-mode .message-input-area {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode input {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.15);
  color: #e8e8e8;
}

.night-mode input::placeholder {
  color: rgba(255, 255, 255, 0.4);
}

.night-mode .message-item {
  color: rgba(255, 255, 255, 0.15);
}

.night-mode .tip-text {
  color: #e8e8e8;
}

.night-mode .bottle-text {
  color: #e8e8e8;
}

.night-mode .floating-bottle {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .stat-number {
  color: #a8c5e0;
}

.night-mode .stat-label {
  color: #a0a0a0;
}

.night-mode .action-card {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .action-text {
  color: #e8e8e8;
}

.night-mode .daily-message {
  background: rgba(255, 255, 255, 0.03);
}

.night-mode .message-content {
  color: #e8e8e8;
}

.night-mode .night-mode-toggle {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .card-front {
  background: rgba(102, 126, 234, 0.2);
  border: 1rpx solid rgba(102, 126, 234, 0.3);
}

.night-mode .card-back {
  background: rgba(118, 75, 162, 0.3);
  border: 1rpx solid rgba(118, 75, 162, 0.4);
}

.night-mode .challenge-box {
  background: rgba(255, 255, 255, 0.05);
}

.night-mode .input-label {
  color: #e8e8e8;
}

.night-mode .message-input {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.15);
  color: #e8e8e8;
}

.night-mode .message-input::placeholder {
  color: rgba(255, 255, 255, 0.4);
}

.night-mode .section-title {
  color: #e8e8e8;
}

.night-mode .history-card {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .history-date {
  color: #a0a0a0;
}

.night-mode .qr-container {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .qr-placeholder {
  background: rgba(255, 255, 255, 0.03);
}

.night-mode .qr-text {
  color: #a0a0a0;
}

.night-mode .share-tip {
  background: rgba(255, 255, 255, 0.03);
}

.night-mode .tip-content {
  color: #e8e8e8;
}

.night-mode .stat-card {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .stat-num {
  color: #a0c4ff;
}

.night-mode .mood-card {
  background: rgba(255, 183, 178, 0.15);
}

.night-mode .mood-text {
  color: #e8e8e8;
}

.night-mode .mood-empty {
  background: rgba(255, 255, 255, 0.03);
}

.night-mode .empty-text {
  color: #a0a0a0;
}

.night-mode .chart-bars {
  background: rgba(255, 255, 255, 0.05);
}

.night-mode .bar-label {
  color: #e8e8e8;
}

.night-mode .bar-container {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .bar-value {
  color: #a0a0a0;
}

.night-mode .achievement-item {
  background: rgba(255, 255, 255, 0.08);
}

.night-mode .achievement-name {
  color: #e8e8e8;
}

.night-mode .achievement-desc {
  color: #a0a0a0;
}

.night-mode .history-list {
  background: rgba(255, 255, 255, 0.05);
}

.night-mode .history-item {
  border-bottom-color: rgba(255, 255, 255, 0.08);
}

.night-mode .history-time {
  color: #a0a0a0;
}

.night-mode .history-content {
  color: #e8e8e8;
}

.night-mode .card {
  background: rgba(255, 255, 255, 0.05);
}
